@model EventDayViewModel

@{
    ViewBag.Title = "Events";
}

<h2>@ViewBag.Title</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(m => m.CreatedBy)</dt>
        <dd>@Html.DisplayFor(m => m.CreatedBy)</dd>

        <dt>@Html.DisplayNameFor(m => m.Name)</dt>
        <dd>@Html.DisplayFor(m => m.Name)</dd>

        <dt>@Html.DisplayNameFor(m => m.EventDate)</dt>
        <dd>@Html.DisplayFor(m => m.EventDate)</dd>

        <dt>@Html.DisplayNameFor(m => m.Total)</dt>
        <dd>@Html.DisplayFor(m => m.Total)</dd>
    </dl>
</div>

<p>
    @if (User.IsInRole("Admin"))
    {
        @Html.ActionLink("Edit", "Edit", new { Model.ID }) @:|
        @Html.ActionLink("Delete", "Delete", new { Model.ID }) @:|
        @Html.ActionLink("Download Orders", "DownloadOrders", new { Model.ID }) @:|
        @Html.ActionLink("Download Product Sales", "DownloadProductSales", new { Model.ID }) @:|
    }
    @Html.ActionLink("Back to List", "Index")
</p>
